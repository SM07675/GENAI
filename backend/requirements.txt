# --- Core server ---------------------------------------------------------
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
websockets>=13.0
pydantic>=2.9.0
pydantic-settings>=2.6.0
python-dotenv>=1.0.0
python-multipart>=0.0.17

# --- AI / LLM client (OpenAI-compatible -> Gemini) -----------------------
openai>=1.55.0
httpx>=0.28.1
google-genai>=1.10.0

# --- Reliability & Observability -----------------------------------------
tenacity>=9.0.0            # retry + exponential backoff with jitter
structlog>=24.4.0          # structured JSON logging with bound context

# --- Speech & GPU Acceleration -------------------------------------------
faster-whisper>=1.0.0       # STT engine (Whisper local transcription)
vosk>=0.3.45                # Wake word detection (offline, no API key)
av>=12.0.0                  # Audio decode (required by faster-whisper)
edge-tts>=6.1.9             # TTS fallback (free, no key needed)
nvidia-cublas-cu12          # GPU CUDA cuBLAS DLLs (cublas64_12.dll)
nvidia-cudnn-cu12           # GPU CUDA cuDNN DLLs (cudnn64_9.dll)
# chatterbox-tts             # optional TTS engine (GPU heavy)
# kokoro                     # optional TTS engine

# --- OS automation tools -------------------------------------------------
numpy>=1.26.0
pyautogui>=0.9.50              # ghost typing / mouse control
psutil>=6.0.0                  # process discovery + force-close
pycaw>=20240210                # Windows core audio (volume control)
comtypes>=1.4.0                # COM bindings required by pycaw
pygetwindow>=0.0.9             # focus windows before ghost-typing
mss>=9.0.0                     # fast screenshots
Pillow>=11.0.0                 # image encode/resize for vision
pyperclip>=1.9.0               # clipboard read/write

# --- Tunneling (mobile access) ------------------------------------------
pyngrok>=7.2.0

# --- Utilities -----------------------------------------------------------
loguru>=0.7.0
requests>=2.32.0
ddgs>=9.0.0
duckduckgo-search>=8.0.0
tavily-python>=0.7.0          # priority-1 web search (advanced depth)
ytmusicapi>=1.8.0

# --- Genie OS Core ---
litellm>=1.0.0
qdrant-client>=1.7.0
mem0ai>=0.0.10
sentence-transformers>=3.0.0
langgraph>=0.0.26
langchain-core>=0.1.27

mss>=9.0.1                 # screen capture for Companion Mode (Windows)
pywin32>=306               # active-window detection for Companion Mode (Windows)
